{
  "name": "grvare-js",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite task-manager",
    "build": "tsc -p task-manager/tsconfig.json --noEmit && vite build task-manager",
    "preview": "vite preview task-manager"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
